var Visitor;
Visitor = (function(){
  Visitor.displayName = 'Visitor';
  var prototype = Visitor.prototype, constructor = Visitor;
  function Visitor(nodes){
    this.nodes = nodes;
  }
  prototype.visit = (function(){
    function visit(node){
      var visitor, _ref;
      visitor = this.nodes[(_ref = (_ref = node.constructor) != null ? _ref.displayName : void 8) != null ? _ref : ""];
      if (visitor == null) {
        throw new Error("No visitor for " + ((_ref = (_ref = node.constructor) != null ? _ref.displayName : void 8) != null
          ? _ref
          : typeof node));
      }
      return visitor.call(this, node);
    }
    return visit;
  }());
  return Visitor;
}());
exports.Visitor = Visitor;